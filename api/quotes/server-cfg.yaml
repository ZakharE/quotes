package: server
generate:
  chi-server: true
  strict-server: true
  models: true
  embedded-spec: true

output-options:
  skip-prune: true
  
compatibility:
  apply-chi-middleware-first-to-last: true
output: ./internal/quotes/server/server.gen.go

import-mapping:
  ./models/models.yaml: "plata_card_quotes/internal/quotes/models"
